import RegisterPage from "@/components/forms/registerform";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <RegisterPage />
    </div>
  );
};

export default page;
